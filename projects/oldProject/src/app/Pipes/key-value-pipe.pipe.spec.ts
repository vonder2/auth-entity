import { KeyValuePipePipe } from './key-value-pipe.pipe';

describe('KeyValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
