export class PhoneBookURLs {

  static urlBase = 'https://contacts-telran.herokuapp.com/api';

  static urlLogin = PhoneBookURLs.urlBase + '/login';
  static urlRegister = PhoneBookURLs.urlBase + '/registration';
  static urlContacts = PhoneBookURLs.urlBase + '/contact';
  static urlAdd = PhoneBookURLs.urlBase + '/contact';
  static urlCorrect = PhoneBookURLs.urlBase + '/contact';
  static urlRemove = PhoneBookURLs.urlBase + '/contact';

}
